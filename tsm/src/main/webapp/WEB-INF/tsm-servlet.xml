<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN 2.0//EN"
        "http://www.springframework.org/dtd/spring-beans-2.0.dtd">
<beans>

	<!-- maps request URLs to Controller names -->
	<bean
		class="org.springframework.web.servlet.mvc.support.ControllerClassNameHandlerMapping">
		<property name="interceptors">
			<list>
				<ref bean="openSessionInViewInterceptor" />
			</list>
		</property>
	</bean>

	<!-- for handling hibernate lazy loading -->
	<bean name="openSessionInViewInterceptor"
		class="org.springframework.orm.hibernate3.support.OpenSessionInViewInterceptor">

		<property name="sessionFactory">
			<ref bean="mySessionFactory" />
		</property>
	</bean>

	<bean id="baseEventController" abstract="true">
		<property name="tsEventDao" ref="tsEventDao" />
	</bean>

	<!-- Controller names are not important when using the above HandlerMapping implementation -->
	<bean class="com.tech4d.tsm.web.SwitchBoardController"
		parent="baseEventController" />

	<!-- this bean with the well known name generates view names for us -->
	<!-- not strictly required since we just want to accept the defaults-->
	<bean id="viewNameTranslator"
		class="org.springframework.web.servlet.view.DefaultRequestToViewNameTranslator" />

	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<bean class="com.tech4d.tsm.web.EventController"
		parent="baseEventController">
		<property name="commandName" value="event" />
		<property name="commandClass"
			value="com.tech4d.tsm.web.TsEventForm" />
		<property name="formView" value="event" />
		<property name="successView"
			value="redirect:switchboard/listEvents.htm" />
	</bean>

	<bean class="com.tech4d.tsm.web.SearchEventController"
		parent="baseEventController">
		<property name="commandName" value="searchEvent" />
		<property name="commandClass"
			value="com.tech4d.tsm.web.SearchEventForm" />
		<property name="formView" value="searchEvent" />
		<property name="successView"
			value="redirect:switchboard/searchEvents.htm" />
	</bean>

</beans>
